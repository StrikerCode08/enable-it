## Getting Started
Intially install all dependencies
```bash
cd INTO_CLONED_LOCATION
npm install
```

First, run the development server:

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
# or
bun dev
```
